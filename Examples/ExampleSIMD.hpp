/* Instruction speficier must be included above library header */
#define CIN_USE_DEFAULT_INSTRUCTION_SET // Force no SIMD (default)
#define CIN_USE_SSE                     // Force SSE use
#define CIN_USE_AVX                     // Force AVX use 
#include "CinMath/CinMath.h"

namespace Code {
    
}